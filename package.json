{
  "name": "energy-bloom-soa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:frontend": "cd frontend && yarn run start",
    "start:backend": "cd backend && yarn run start:all",
    "start:all": "concurrently \"yarn run start:frontend\" \"yarn run start:backend\"",
    "build:frontend": "cd frontend && yarn run build",
    "build:backend": "cd backend && yarn run build",
    "build": "concurrently \"yarn run build:frontend\" \"yarn run build:backend\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "cookie": "^0.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
